const nomes = ['joão', 'maria', 'ana', 'luiz']

const cidades = ['recife', 'camaragibe', 'olinda', 'jaboatão']

const texto = "Olá Node!"

function adicionar(a, b) {
	return a + b
}

module.exports = {nomes, cidades, texto, adicionar}
